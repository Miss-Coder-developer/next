// Third-party imports
import React from "react"

// Global imports

// Local imports
import { Container, HeaderTitle } from "./styles"

////////////////////////////////////////////////////////////////////////////////

/**
 * @function Header
 * @category Layout
 * @description Top bar containing the main options concerning the current page.
 */
const Header = ({ title, children }) =>
	null
	// <Container as="header">
	// 	<HeaderTitle>{title}</HeaderTitle>
	// 	{children}
	// </Container>

export default Header
